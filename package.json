{
  "name": "dynamic-mock-server",
  "version": "1.0.0",
  "description": "Dynamic mock server with JSON persistence and per-IP rate limiting",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
  "express": "^4.18.2",
  "morgan": "^1.10.0"
  }
}
